interface:
  display_name: "Bytebot"
  short_description: "Repo-specific patterns and workflows for bytebot"
  default_prompt: "Use the bytebot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true